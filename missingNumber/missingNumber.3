public int missingNumber(int[] nums) {
//First approch using hashset
     HashSet<Integer> set=new HashSet<>();
     for(int num: nums){
         set.add(num);
     }
     for(int i=1; i<=nums.length; i++){
         if(!set.contains(i)){
             return i;
            }
        }
     return -1;
}